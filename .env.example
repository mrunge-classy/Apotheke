# Public site URL, used for metadata, OpenGraph tags and the sitemap.
NEXT_PUBLIC_SITE_URL=https://apotheke.zugriff.at

# Port for `next start` (see ecosystem.config.js in DEPLOYMENT.md). Optional, defaults to 3000.
# PORT=3000
